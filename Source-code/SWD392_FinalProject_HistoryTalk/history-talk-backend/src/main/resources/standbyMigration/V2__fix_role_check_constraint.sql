-- -- Drop old check constraint generated by Hibernate (USER | STAFF | ADMIN)
-- ALTER TABLE historical_schema."user" DROP CONSTRAINT IF EXISTS user_role_check;
--
-- -- Add new check constraint with CUSTOMER instead of USER
-- ALTER TABLE historical_schema."user"
--     ADD CONSTRAINT user_role_check CHECK (role IN ('CUSTOMER', 'STAFF', 'ADMIN'));
--
-- -- Migrate any existing rows that still have the old USER value
-- UPDATE historical_schema."user" SET role = 'CUSTOMER' WHERE role = 'USER';
